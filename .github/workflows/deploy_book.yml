name: deploy-book

# Only run this when the master branch changes or on pull requests
on:
  push:
    branches:
    - main
    - gh-test
  pull_request:
    branches:
    - main
    # If your git repository has the Jupyter Book within some-subfolder next to
    # unrelated files, you can make this run only if a file within that specific
    # folder has been modified.
    #
    # paths:
    # - some-subfolder/**

# This job installs dependencies, builds the book, and pushes it to `gh-pages`
jobs:
  deploy-book:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5

    # Set up Python
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
        check-latest: true
    
    # Install uv
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"

    # Install dependencies and build the book
    - name: Install dependencies
      run: uv sync
      
    - name: Build the book
      run: uv run jupyter book build --all .
      
    - name: Verify build output
      run: |
        if [ -d "_build/html" ]; then
          echo "Build successful. Directory structure:"
          tree -L 2 _build/html/ || find _build/html/ -maxdepth 2 -type f | head -20
        else
          echo "Build failed: _build/html not found"
          echo "Current directory structure:"
          tree -L 3 . || find . -maxdepth 3 -type d | head -20
          exit 1
        fi

    # Push the book's HTML to github-pages (only on push, not on PR)
    - name: GitHub Pages action
      if: github.event_name == 'push'
      uses: peaceiris/actions-gh-pages@v3.6.1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./_build/html
